# adapters

Platform and notification adapters. See ../docs/adapter-contract.md for what
varies and what must stay fixed.

- `telegram/` — platform adapter, inbound **and** outbound.
- `cmux/` — notifier adapter.
